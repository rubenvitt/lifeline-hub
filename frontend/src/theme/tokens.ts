import type { ThemeConfig } from 'antd';

/** Dezentes Branding über antd Design-Tokens (Spec §13), modusunabhängig. */
export const baseToken: ThemeConfig['token'] = {
  colorPrimary: '#a8071a',
  borderRadius: 4,
};
