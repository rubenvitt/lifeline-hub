# Prüfliste Einsatztauglichkeit — Betroffene: Zustand, Fundort, Verbleib (LFH-613)

Gate 7 der Bedien-Leitlinie (`2026-07-25-bedien-leitlinie-einsatzkontexte.md`, Festlegung 7)
verlangt diese Liste an jeder neuen oder umgebauten Seite; ein Modul-Task ohne ausgefüllte
Prüfliste gilt nicht als fertig. Anlass ist Aufgabe 7.4 in
`openspec/changes/lfh-613-betroffene-zustand-fundort-verbleib/tasks.md`.

## Geltungsbereich

| Angabe | Wert |
| --- | --- |
| Routen | `/einsaetze/:id/personen` (Zeilen; Karte über `?ansicht=karte`), `/einsaetze/:id/personen/aufnahme`, `/einsaetze/:id/personen/:personId`, `/einsaetze/:id/lage-dashboard` |
| Stand | Commit `e4665ed3` auf `feat/lfh-613-betroffene-zustand-fundort-verbleib` (nach dem Rebase auf `alpha`) |
| Zielkontext | Fükw 1366 × 768 px, Tastatur + Maus, Stufe `kompakt`; Führungs-Tablet in `komfortabel`; mobil ~390 px (Leitlinie, vier Kontexte) |
| Nicht enthalten | Personen als Ebene der Lagekarte (Non-Goal laut `design.md`, Folgetask aus Aufgabe 7.2); Backend-Vertrag (Migrationen, API) — der ist über `cargo test` belegt, nicht über diese Liste |

Die Liste bewertet nur, was LFH-613 **neu gebaut oder umgebaut** hat. Bestandsteile derselben
Seiten (Sichtungsraster, Abgleichspalte, Statuswechsel, übrige Kennzahlen) haben ihre eigenen
Prüflisten und werden hier nicht neu bewertet.

| Fläche | Stellvertreter | Browser-Messung Gate 3 / Kontrast |
| --- | --- | --- |
| **1 · Betroffenenliste** — Spalte „Zustand" (inline), Koordinate in der Fundort-Spalte | `personen/personenSpalten.tsx`, `components/BemerkungZelle.tsx` (Wort `bezeichnung`), `pages/PersonenPage.tsx` | nein |
| **2 · Schnellerfassungszeile** — Kürzel `#lat/lon` | `personen/personBefehl.ts`, `personen/koordinate.ts`, `personen/BetroffeneZeile.tsx` | nein |
| **3 · Maske „Weitere Angaben" + Detailseite** — Zustand, Koordinate, „vermisst seit"; Bearbeiten und „Auf Lagekarte verorten" | `personen/AufnahmeFelder.tsx`, `personen/LagedatenFelder.tsx`, `personen/PersonErfassungModal.tsx`, `pages/personen/AufnahmePage.tsx`, `pages/PersonenDetailPage.tsx`, `personen/personBearbeiten.ts`, Platzier-Zweig in `pages/lagekarte/useKartenInteraktion.ts` | nein |
| **4 · Kartenansicht der Betroffenen** | `personen/BetroffeneKarte.tsx`, `personen/personenKarte.ts`, Donut-Segment `person` in `pages/lagekarte/clusterDonut.ts` | nein (nur Marker in der Quelle, siehe Nachweise) |
| **5 · Lage-Dashboard** — Fuß „Transportiert / offen", Notiz „n seit über 4 h" | `pages/lage-dashboard/LagePaneele.tsx`, `pages/lage-dashboard/lagebild.ts`, `personen/personenBilanz.ts` (`transportBilanz`) | nein (für die neuen Teile) |

**Es gibt für LFH-613 keine Gate-3-Trefflächenmessung und keine Kontrastmessung der neuen
Elemente** (Zustand-Knopf, Karten-Hinweis, Verorten-Link, Marker und ihre Beschriftung). Jede
Zeile, die daran hängt, ist offen und steht unten unter „Nachzüge".

**Verdikte:** **erfüllt** (nur mit Beleg: Testdatei + Testname, oder Messung + Commit) ·
**offen → Zielticket** · **nicht anwendbar** (nur mit Begründung). „Nicht geprüft" ist keins.
Gerechnetes und aus Quelltext/Grep Geschlossenes trägt **[abgeleitet]**. Eine Annahme wie
„erbt vom `ConfigProvider`" ist kein Beleg (LFH-396).

---

## Die Nachweise

| Nachweis | Ergebnis | Stand |
| --- | --- | --- |
| `e2e/betroffene-karte.spec.ts`, „Betroffene mit Koordinate stehen als Marker auf der Karte, die Lücke wird gezählt" | grün, 1 passed. Prüft: `#52.2691/9.1342` aus der Schnellerfassungszeile landet im **selben** Anlege-POST (Serverstand), die Kartenansicht lädt nach Wahl in der Segmentleiste, beide verorteten Personen stehen als `person-<id>` in der MapLibre-Quelle, die Person ohne Koordinate nicht, und der Hinweis lautet „1 Person ohne Koordinate — nicht auf der Karte". Geprüft wird die **Quelle**, nicht die Optik — keine Größe, keine Farbe, keine Beschriftung | Lauf 22.09.2026, Branch-Stand vor dem Rebase auf `alpha` (Spec aus `f41effa0`) |
| `e2e/betroffene-kontrast.spec.ts` + `e2e/lagekarte-smoke.spec.ts` | grün, 7 passed. Die Kontrast-Spec rendert die Liste `/personen` bei 1440 px in Tag und Nacht **mit** der neuen Zustand-Spalte, misst aber nur die SK-Auswahlflächen und die fünf Personenstatus-Tags — **keines** der neuen Elemente. Die Lagekarten-Smoke belegt, dass der Filter `m.typ !== 'person'` (`LagekartePage.tsx`) die Lagekarte nicht bricht | Lauf 22.09.2026, gleicher Stand |
| Gesamt-Gate `./scripts/check-all.sh` (Aufgabe 7.3) | **läuft beim Schreiben dieser Liste, Ergebnis steht aus.** Die unten zitierten Vitest-Tests sind per Grep am Stand `e4665ed3` gefunden (Datei + Name). Die drei e2e-Specs oben sind nach dem Rebase nicht erneut gelaufen | `e4665ed3` |
| Grep über die neuen und geänderten Produktivdateien (`personen/BetroffeneKarte.tsx`, `personenKarte.ts`, `LagedatenFelder.tsx`, `AufnahmeFelder.tsx`, `personenSpalten.tsx`, `BetroffeneZeile.tsx`, `personBefehl.ts`, `personBearbeiten.ts`, `koordinate.ts`, `lage-dashboard/LagePaneele.tsx`, `lage-dashboard/lagebild.ts`, `components/BemerkungZelle.tsx`) sowie die LFH-613-Hunks in `PersonenPage.tsx`/`PersonenDetailPage.tsx` | `animation`/`blink`/`keyframes`/`transition` **0** · `size=` **0** (einzige Angabe in den Hunks: `Space size="middle"` an der Koordinate der Detailseite, nicht interaktiv) · `danger` **0** · Farbliterale (`#…`, `rgb(`, `color=`) **0** · `Popconfirm`/`modal.confirm` **0** · Toast-Aufrufe genau **einer**: `message.error` in `personenSpalten.tsx:170` (Zustand nicht gespeichert) | `e4665ed3` [abgeleitet] |
| Grep `sticky`/`position: fixed` über `PersonenPage.tsx`, `PersonenDetailPage.tsx`, `components/instrument/Schnellerfassungszeile.tsx`, `pages/lage-dashboard/*.tsx` | **0** in diesen Dateien. Das Primitiv `components/KatalogTabelle.tsx` ist bewusst **nicht** mitgegrept: es setzt antds `sticky` (`KatalogTabelle.tsx:541`) und trägt damit die stehende Kopfzeile der Betroffenenliste (Tabelle 1, Nr. 13) | `e4665ed3` [abgeleitet] |
| Außerhalb der Tokens: `clusterDonut.ts` erhält `person: '#be185d'` (Rosé) in der Legacy-Palette der Cluster-Donuts | einziges neues Farbliteral des Tickets; die Lagekarten-Prüfliste (2026-07-30, Nr. 5) hat diese Palette als kartografische Werte hingenommen, eine Doppelbelegungsprüfung gegen die Sichtungsfarben gibt es nicht | `e4665ed3` [abgeleitet] |

Übernommene Messungen anderer Tickets, soweit sie dieselbe Rolle am selben Primitiv treffen:
LFH-606 [M3] (Notiz der `Kennzahl` in `gedaempft`: Tag 8,42, Nacht 7,27) und LFH-618 Nr. 8
(`schwach` am Tag 5,33 auf `grund`, 5,84 auf `paneel` — offen → LFH-643; Platzhalter
`colorTextPlaceholder` = `schwach`, Tag 6,37 auf Weiß, nachts 5,03).

---

## Tabelle 1 — Betroffenenliste: Spalte „Zustand" und Koordinate im Fundort

| Nr. | Kriterium | Verdikt | Beleg / Begründung | Zielticket |
| --- | --- | --- | --- | --- |
| 1 | **Treffläche** — Boden ≥ 24 × 24 CSS px oder 24-px-Umkreis frei (WCAG 2.5.8 AA); zeitkritische Aktion ≥ 48 × 48 px mit ≥ 8 px Abstand (WCAG 2.5.5 / Material 48 dp). | **offen** | Nicht gemessen. Entlastend **[abgeleitet]**: der leere Zustand ist ein antd-`Button` `type="link"` ohne `size`-Angabe (`BemerkungZelle.tsx`, Grep `size=` 0) — dieselbe Bauform, die LFH-369 wegen der geerbten `controlHeight` gewählt hat. **Ohne Ableitung** bleibt der gefüllte Zustand: sein Bedienziel ist der Stift von antds `Typography` `editable`, ein Icon-Knopf, dessen Fläche keine Zeile im Repo misst. Keine zeitkritische Aktion | LFH-650 |
| 2 | **Handschuh-Modus** vorhanden und geprüft — Zeilenhöhe ≥ 72 px (= 19,05 mm [abgeleitet], MIL-STD-1472F Fig. 12), Abstand ≥ 16 px [abgeleitet aus Fig. 24, last contact]. | **offen** | Weder Höhe des Platzhalter-Knopfs noch des Stifts in `handschuh` gemessen, ebenso wenig der Abstand zum Nachbarziel (Sichtungs-Spalte, Zeilenklick) | LFH-650 |
| 3 | **Rückmeldung vor der Serverantwort** — sichtbar ≤ 100 ms (MIL 5.4.6.4); Kommandoreaktion ≤ 2 s (Tab. XXII); > 15 s nur mit Fortschrittsmeldung (MIL 5.14.9). | **offen** | **Trägt:** der Schreibweg schickt genau das eine Feld und nichts bei Wertgleichheit — `personenSpalten.test.tsx` „bearbeitet den leeren Zustand in der Zeile: PATCH mit NUR dem Zustand (Spec-Szenario)", „schickt nichts, wenn der Wert unverändert bleibt (Wertgleichheits-Riegel)". **Offen [abgeleitet]:** die Zelle hat **keinen** Laufzustand. `ZustandSchreiben` ruft `mutation.mutate` ohne Rückkanal, `BemerkungZelle` kennt keine `laeuft`-Angabe; der neue Wert erscheint erst nach der Invalidierung. Bis dahin steht bei einem vorher leeren Feld wieder „Zustand hinzufügen" da — die Eingabe wirkt verworfen. Das Muster stammt aus den Kräfte-Listen (`FahrzeugePage.tsx`, `bemerkungMutation`); ein Präzedenzfall ist kein Beleg für sichtbare Rückmeldung | LFH-650 |
| 4 | **Kritische Aktion hat eine zweite Handlung** — Storno, Abschluss, Löschen, Alarmierung: je 1 zusätzliche Bestätigung (MIL 5.4.6.6). | **nicht anwendbar** | Der Zustand ist eine umkehrbare Kurznotiz: überschreiben oder leeren (`''` → `null`, `personenSpalten.tsx`) stellt jeden Stand wieder her (LFH-378 „erst die Umkehrbarkeit"). Die Koordinate ist hier nur Anzeige | — |
| 5 | **Kontrast in beiden Modi** — Text Tag ≥ 7 : 1, Nacht ≥ 5 : 1, nie < 4,5 : 1 (MIL 5.2.4.2.2.2; WCAG 1.4.6/1.4.3); Zustände, Rahmen, Fokusring ≥ 3 : 1 (WCAG 1.4.11). | **offen** | Ungemessen: Platzhalter-Knopf (Bedienrolle), Zustandstext (`text2`, 12 px), Koordinate (`gedaempft`, Mono 11 px) unter dem Fundort-Freitext. Die Kontrast-Spec rendert diese Route mit der neuen Spalte, misst aber nur die Status-Tags (Nachweise). Der Strich „—" der Lesezelle steht in `schwach` und fällt unter den LFH-618-Befund (Tag 5,33 / 5,84) | LFH-650 · LFH-643 |
| 6 | **Kein Status allein über Farbe** — jede Statusfarbe zusätzlich mit Text, Symbol oder Form, 0 Ausnahmen (WCAG 1.4.1 Level A; MIL 5.4.6.8; 1 von 12 Männern, NEI). | **erfüllt** | Beide neuen Angaben sind Wörter bzw. Zahlen ohne Farbträger: `personenSpalten.test.tsx` „zeigt Freitext und darunter die Koordinate in Mono", „zeigt die Koordinate allein ohne „—", wenn der Freitext fehlt", „bleibt ohne Schreibrecht reine Anzeige — Wert oder „—", kein Knopf"; die Fundort-Lücke schließt Freitext **oder** Koordinate: `personenBilanz.test.ts` „schließt die Fundort-Lücke mit Freitext ODER Koordinate (LFH-613)" | — |
| 7 | **Eine Farbe = eine Bedeutung** — Palette auf Doppelbelegung geprüft, gesättigte Farbe nur für abnorme Zustände, Grundfläche aus A0 (weder `#000000` noch `#ffffff`) — ASM Consortium. | **erfüllt [abgeleitet]** | Keine neue Farbe: Farbliterale 0, `danger` 0 (Grep `e4665ed3`); der Platzhalter-Knopf trägt die blaue Bedienrolle (antd `type="link"`), Zustand und Koordinate Textrollen | — |
| 8 | **Helligkeits-/Kontrastregler** vorhanden und bei aktiver Warnung nicht bis AUS dimmbar — 1 Regler, 1 Sperre (MIL 5.2.2.1.9, 5.2.4.2.2.3). | **offen** | App-weite Lücke, es gibt nirgends einen Regler | LFH-397 |
| 9 | **Kritische Anzeigen im Blickfeld** — innerhalb 15° der normalen Blickachse, nicht am Layoutrand (MIL 5.2.2.1.7). | **offen** | Ein gescheitertes Speichern meldet sich **nur** über `message.error` (`personenSpalten.tsx:170`) — nach rund drei Sekunden weg, am oberen Rand statt an der Zeile, und der Wert springt ohne Hinweis auf den alten zurück (kein optimistisches Update). Das ist der H14-Befund aus LFH-345 („ein Speicherfehler gehört an die Seite"); dort ist er für Formulare gelöst, für Inline-Zellen nicht. Wahrscheinlicher Befund, kein Messwert | LFH-650 |
| 10 | **Alarmbudget eingehalten** — 1–2 je 10 min im Dauerbetrieb, < 10 in den ersten 10 min einer Großlage, ≤ 10 je 10-min-Fenster, ~80/15/5 %, 0 flatternde Alarme, ≤ 3 Eskalationsstufen (EEMUA 191 S. 96/97; ISA-18.2). | **nicht anwendbar** | Die Fläche erzeugt keine ungefragte Meldung: der einzige Toast ist die Fehlerantwort auf eine eigene Handlung (Grep), nie ein Live-Ereignis. Dass er am falschen Ort steht, ist Nr. 9 | — |
| 11 | **Warnverhalten** — kein Blinken auf lesbarem Text, ≤ 2 Blinkraten (schnellere ≤ 5 Hz, langsamere ≥ 0,8 Hz), jede Warnung quittierbar, jeder Ton mit visueller Entsprechung (MIL 5.2.1.5.5.3/.4/.5, 5.3.6.3). | **erfüllt [abgeleitet]** | Kein Blinken und keine Bewegung (Grep `animation`/`blink`/`keyframes`/`transition` 0), kein Ton | — |
| 12 | **Kein Sprung unter dem Cursor** — CLS ≤ 0,1 (75. Perzentil, web.dev); neue Datensätze nur als opt-in-Sammelbanner (WCAG 3.2.5 / G76). | **offen** | **Trägt:** die Spalte hat eine feste Breite (140 px, keine weitere Fließspalte) und steht fest nach der Sichtung: `personenSpalten.test.tsx` „trägt die Spalten des Entwurfs — „Zustand" nach der Sichtung (LFH-613)"; der Zufluss neuer Zeilen läuft weiter über das Sammelbanner der `Datensicht` (Bestand). **Offen [abgeleitet]:** nach dem ersten Speichern wechselt die Zelle vom Knopf (`controlHeight`, in `handschuh` 72 px) zu blankem Text — `BemerkungZelle.tsx` hält selbst fest, dass die beiden „nicht gleich hoch" sind. Die Zeile schrumpft dann in der Stufe `handschuh` sichtbar, und alle Zeilen darunter rücken nach. Ebenso live: die Fundort-Zelle wird mit einer Koordinate zweizeilig. Kommt die Koordinate bei offener Liste dazu (Verorten auf der Lagekarte invalidiert `einsatzKeys.personen`, `useKartenInteraktion.ts`; oder `#lat/lon` einer zweiten Stelle), wächst die Zeile, und alles darunter rückt nach | LFH-650 |
| 13 | **Fokus nie verdeckt** — 0 vollständig verdeckte Fokusziele beim Tab-Durchlauf hinter fixierten Köpfen, Fußleisten oder Drawern (WCAG 2.4.11 AA). | **offen** | Mit der Spalte kommt je Zeile ein neuer Tab-Stopp dazu, und die Tabelle trägt die stehende Kopfzeile von `KatalogTabelle`. Ob ein Fokusziel beim Rückwärts-Tabben dahinter verschwindet, ist wie bei allen Katalogtabellen nicht gemessen | LFH-373 |
| 14 | **Tabellenseite vollständig** — fixierte Kopfzeile, fixierte menschenlesbare Identifierspalte, umschaltbarer Spaltensatz mit Zähler ausgeblendeter Spalten, keine Auflösung in Karten, wo verglichen wird (NN/g Data Tables / Mobile Tables). | **erfüllt [abgeleitet]** | Die Liste bleibt eine `Datensicht` mit Spaltenschalter und Zähler; die Kennung ist die Registriernummer (`key: 'reg'`, `immerSichtbar: true`, `personenSpalten.tsx`), nicht die DB-`id`. Die neue Spalte trägt kein `abBreite` und läuft damit durch denselben Zähler wie alle anderen. Der Kartenzweig unter `md` ist die bestehende, begründete Ausnahme (Dateikopf `Datensicht.tsx`); die neue **Kartenansicht** ist eine Geo-Ansicht zusätzlich zur Tabelle (Tabelle 4, Nr. 14), keine Auflösung in Karten | — |
| 15 | **Erfassungsmaske vollständig** — Defaults vorbelegt, sichtbar und einzeln überschreibbar (MIL 5.14.7.1/.3), „Speichern und nächsten anlegen" mit gehaltenem Kontext (5.14.7.4), Sammelliste mit Ändern/Entfernen je Zeile (DWP „Add another thing"), Labels über dem Feld (50 ms statt 500 ms Sakkade, Penzo), volle Tastaturbedienung (WCAG 2.1.1). | **nicht anwendbar** | Keine Erfassungsmaske: ein inline bearbeitbares Einzelfeld einer bestehenden Zeile. Die Masken sind Fläche 2 und 3 | — |

**Verdikt-Bilanz:** 4 erfüllt · 8 offen · 3 nicht anwendbar.

---

## Tabelle 2 — Schnellerfassungszeile `/person` mit `#lat/lon`

| Nr. | Kriterium | Verdikt | Beleg / Begründung | Zielticket |
| --- | --- | --- | --- | --- |
| 1 | **Treffläche** — Boden ≥ 24 × 24 CSS px oder 24-px-Umkreis frei (WCAG 2.5.8 AA); zeitkritische Aktion ≥ 48 × 48 px mit ≥ 8 px Abstand (WCAG 2.5.5 / Material 48 dp). | **nicht anwendbar** | Kein neues Bedienziel: die Koordinate ist ein Kürzel im bestehenden Eingabefeld, die erkannte Marke ein nicht-interaktives `<span>` (`BetroffeneZeile.tsx`, `case 'koordinate'`). Das Feld selbst ist Bestand (Schnellerfassungs-Prüfliste 2026-07-29) | — |
| 2 | **Handschuh-Modus** vorhanden und geprüft — Zeilenhöhe ≥ 72 px (= 19,05 mm [abgeleitet], MIL-STD-1472F Fig. 12), Abstand ≥ 16 px [abgeleitet aus Fig. 24, last contact]. | **nicht anwendbar** | Wie Nr. 1: kein neues Ziel | — |
| 3 | **Rückmeldung vor der Serverantwort** — sichtbar ≤ 100 ms (MIL 5.4.6.4); Kommandoreaktion ≤ 2 s (Tab. XXII); > 15 s nur mit Fortschrittsmeldung (MIL 5.14.9). | **erfüllt** | Die Koordinate wird schon beim Tippen erkannt und als Marke gezeigt, vor jedem Request, und geht im selben offlinefähigen POST mit: `PersonenPage.test.tsx` „schickt die Koordinate aus „#lat/lon" im SELBEN POST (LFH-613, Spec-Szenario)" (prüft `[data-lfh="erkannt"]` = `#52.2691/9.1342` vor dem Enter); Quittung, leeres Feld, Fokus bleibt: „erfasst in Serie: EIN POST mit Sichtung und client_id, Feld leer, Fokus bleibt, Quittung aus der Antwort"; Serverstand im Browser: `e2e/betroffene-karte.spec.ts` (Nachweise) | — |
| 4 | **Kritische Aktion hat eine zweite Handlung** — Storno, Abschluss, Löschen, Alarmierung: je 1 zusätzliche Bestätigung (MIL 5.4.6.6). | **nicht anwendbar** | Die Anlage einer Person ist keine der genannten Aktionen; eine Fehlanlage wird storniert, die Koordinate ist auf der Detailseite änderbar und leerbar (Tabelle 3) | — |
| 5 | **Kontrast in beiden Modi** — Text Tag ≥ 7 : 1, Nacht ≥ 5 : 1, nie < 4,5 : 1 (MIL 5.2.4.2.2.2; WCAG 1.4.6/1.4.3); Zustände, Rahmen, Fokusring ≥ 3 : 1 (WCAG 1.4.11). | **offen** | Die Koordinaten-Marke steht in `gedaempft`, Mono 12 px; ungemessen in beiden Modi. Die Ablehnung nutzt die gemessene Rolle `alarmText` (LFH-618) — für die neue Marke gilt das nicht | LFH-650 |
| 6 | **Kein Status allein über Farbe** — jede Statusfarbe zusätzlich mit Text, Symbol oder Form, 0 Ausnahmen (WCAG 1.4.1 Level A; MIL 5.4.6.8; 1 von 12 Männern, NEI). | **erfüllt** | Jede Ablehnung ist ein Satz mit Grund: `PersonenPage.test.tsx` „sendet bei unerkanntem Kürzel NICHT und sagt warum; der Wortlaut bleibt stehen" (Eingabe `#52.1`, Meldung „Koordinate unbrauchbar"); `personBefehl.test.ts` „meldet eine zweite Koordinate, statt die erste still zu überschreiben" und die fünf unbrauchbaren Fälle `'#52.2691', '#95/9', '#52/200', '#', '#abc/9'` | — |
| 7 | **Eine Farbe = eine Bedeutung** — Palette auf Doppelbelegung geprüft, gesättigte Farbe nur für abnorme Zustände, Grundfläche aus A0 (weder `#000000` noch `#ffffff`) — ASM Consortium. | **erfüllt [abgeleitet]** | Die Marke trägt eine Textrolle, keine Statusfarbe; Farbliterale 0 (Grep `e4665ed3`) | — |
| 8 | **Helligkeits-/Kontrastregler** vorhanden und bei aktiver Warnung nicht bis AUS dimmbar — 1 Regler, 1 Sperre (MIL 5.2.2.1.9, 5.2.4.2.2.3). | **offen** | App-weite Lücke, kein Regler | LFH-397 |
| 9 | **Kritische Anzeigen im Blickfeld** — innerhalb 15° der normalen Blickachse, nicht am Layoutrand (MIL 5.2.2.1.7). | **erfüllt** | Eine unbrauchbare Koordinate sperrt das Absenden und steht als `role="alert"` direkt unter dem Feld, der Wortlaut bleibt im Feld: `PersonenPage.test.tsx` „sendet bei unerkanntem Kürzel NICHT und sagt warum; der Wortlaut bleibt stehen" | — |
| 10 | **Alarmbudget eingehalten** — 1–2 je 10 min im Dauerbetrieb, < 10 in den ersten 10 min einer Großlage, ≤ 10 je 10-min-Fenster, ~80/15/5 %, 0 flatternde Alarme, ≤ 3 Eskalationsstufen (EEMUA 191 S. 96/97; ISA-18.2). | **nicht anwendbar** | Keine Meldung: Quittung und Ablehnung stehen inline an der Zeile, kein Toast (Grep) | — |
| 11 | **Warnverhalten** — kein Blinken auf lesbarem Text, ≤ 2 Blinkraten (schnellere ≤ 5 Hz, langsamere ≥ 0,8 Hz), jede Warnung quittierbar, jeder Ton mit visueller Entsprechung (MIL 5.2.1.5.5.3/.4/.5, 5.3.6.3). | **erfüllt [abgeleitet]** | Kein Blinken, kein Ton (Grep 0) | — |
| 12 | **Kein Sprung unter dem Cursor** — CLS ≤ 0,1 (75. Perzentil, web.dev); neue Datensätze nur als opt-in-Sammelbanner (WCAG 3.2.5 / G76). | **offen** | Die Zeile „erkannt: …" unter dem Feld bricht um (`flexWrap: 'wrap'`, `BetroffeneZeile.tsx`), und die Koordinate ist mit `#52.2691/9.1342` die längste Marke. Auf ~390 px kommt beim Tippen voraussichtlich eine Zeile dazu, und die Liste darunter rückt nach. Wahrscheinlicher Befund, nicht gemessen | LFH-650 |
| 13 | **Fokus nie verdeckt** — 0 vollständig verdeckte Fokusziele beim Tab-Durchlauf hinter fixierten Köpfen, Fußleisten oder Drawern (WCAG 2.4.11 AA). | **erfüllt [abgeleitet]** | Die Zeile steht **über** der Tabelle und außerhalb von `KatalogTabelle`, sie selbst ist nicht fixiert (Grep `sticky`/`fixed` in `Schnellerfassungszeile.tsx` = 0); die stehende Tabellenkopfzeile liegt darunter und betrifft Tabelle 1, Nr. 13. Der Fokus bleibt im Feld (Test Nr. 3) | — |
| 14 | **Tabellenseite vollständig** — fixierte Kopfzeile, fixierte menschenlesbare Identifierspalte, umschaltbarer Spaltensatz mit Zähler ausgeblendeter Spalten, keine Auflösung in Karten, wo verglichen wird (NN/g Data Tables / Mobile Tables). | **nicht anwendbar** | Eingabezeile, keine Menge | — |
| 15 | **Erfassungsmaske vollständig** — Defaults vorbelegt, sichtbar und einzeln überschreibbar (MIL 5.14.7.1/.3), „Speichern und nächsten anlegen" mit gehaltenem Kontext (5.14.7.4), Sammelliste mit Ändern/Entfernen je Zeile (DWP „Add another thing"), Labels über dem Feld (50 ms statt 500 ms Sakkade, Penzo), volle Tastaturbedienung (WCAG 2.1.1). | **erfüllt** | **Trägt:** Serienerfassung mit Enter, Feld leer, Fokus bleibt (Test Nr. 3); volle Tastaturbedienung, die Koordinate ist Text; deutsches Dezimalkomma und Minus: `personBefehl.test.ts` „nimmt das deutsche Dezimalkomma und Minus", „beendet einen „@"-Suchtext an der Koordinate", „bringt die Koordinate durch loeseBefehl in die Anlage". **Nachgezogen nach dem ersten Entwurf dieser Liste:** der Kürzel-Hinweis im Leerzustand nennt jetzt `#Koordinate (52.2691/9.1342)` neben Name, `m/w/d + Alter`, `sk…` und `@UHS` (`BetroffeneZeile.tsx`, Wortlaut des Entwurfs S7) — `PersonenPage.test.tsx` „schickt die Koordinate aus „#lat/lon" im SELBEN POST" prüft den Hinweis vor dem Tippen | — |

**Verdikt-Bilanz:** 7 erfüllt · 3 offen · 5 nicht anwendbar.

---

## Tabelle 3 — Maske „Weitere Angaben" und Detailseite (Bearbeiten, „Auf Lagekarte verorten")

| Nr. | Kriterium | Verdikt | Beleg / Begründung | Zielticket |
| --- | --- | --- | --- | --- |
| 1 | **Treffläche** — Boden ≥ 24 × 24 CSS px oder 24-px-Umkreis frei (WCAG 2.5.8 AA); zeitkritische Aktion ≥ 48 × 48 px mit ≥ 8 px Abstand (WCAG 2.5.5 / Material 48 dp). | **offen** | **[abgeleitet], nicht gemessen:** die drei neuen Felder sind antd `Input`/`DatePicker` ohne `size` (Grep 0). Die Stufe trägt sich route-unabhängig in eine antd-Steuerfläche (`e2e/dichte.spec.ts`, Ableitung wie LFH-46, Tabelle 3, Nr. 1). **Nicht ableitbar** ist „Auf Lagekarte verorten": ein handgebauter `<Link>` in einer `Descriptions`-Zelle. `personBearbeiten.test.ts` „trägt beide Angaben des handgebauten Bedienziels und folgt der Dichtestufe" prüft den **Inline-Style** (`minHeight` + `paddingInline`, `inline-flex`), keinen Pixel. LFH-396 hat genau so einen Anker mit 17 px in jeder Stufe gemessen | LFH-650 |
| 2 | **Handschuh-Modus** vorhanden und geprüft — Zeilenhöhe ≥ 72 px (= 19,05 mm [abgeleitet], MIL-STD-1472F Fig. 12), Abstand ≥ 16 px [abgeleitet aus Fig. 24, last contact]. | **offen** | Keine Messung in `handschuh`, weder die Felder noch der Link. Der Abstand zwischen Koordinatentext und Link kommt aus `Space size="middle"` = `abstand.md` 11 / 18 / 26 px je Stufe **[abgeleitet]** (CLAUDE.md, LFH-363), in `handschuh` also rechnerisch über 16 px | LFH-650 |
| 3 | **Rückmeldung vor der Serverantwort** — sichtbar ≤ 100 ms (MIL 5.4.6.4); Kommandoreaktion ≤ 2 s (Tab. XXII); > 15 s nur mit Fortschrittsmeldung (MIL 5.14.9). | **erfüllt [abgeleitet]** | Maske: `laeuft` → `loading` am Primärknopf der Erfassungshülle (Bestand, Ableitung wie LFH-46); Detailseite: `loading={editMutation.isPending}` am Speichern-Knopf (`PersonenDetailPage.tsx:797`). Verorten: der Klick auf die Karte schickt genau das Koordinatenpaar und quittiert mit „Objekt verortet": `useKartenInteraktion.test.tsx` „Platzier-Auftrag person: Klick PATCHt NUR die Fundort-Koordinate und invalidiert die Personen". Der Ladezustand selbst ist für die neuen Felder nicht eigens getestet | — |
| 4 | **Kritische Aktion hat eine zweite Handlung** — Storno, Abschluss, Löschen, Alarmierung: je 1 zusätzliche Bestätigung (MIL 5.4.6.6). | **nicht anwendbar** | Zustand, Koordinate und „vermisst seit" sind umkehrbar: erneut setzen oder leeren (`PersonenDetailPage.test.tsx` „leert die Koordinate als Paar"), Verorten ebenso (erneut platzieren). Das Bearbeiten läuft weiter über den CAS-Dialog des Bestands | — |
| 5 | **Kontrast in beiden Modi** — Text Tag ≥ 7 : 1, Nacht ≥ 5 : 1, nie < 4,5 : 1 (MIL 5.2.4.2.2.2; WCAG 1.4.6/1.4.3); Zustände, Rahmen, Fokusring ≥ 3 : 1 (WCAG 1.4.11). | **offen** | Ungemessen: Verorten-Link (Bedienrolle), Koordinate im Lesemodus (`fontFamilyCode`), Feldmeldungen „Liegt in der Zukunft" / Koordinatengrund, der `extra`-Text „Ohne Angabe gilt der Zeitpunkt der Meldung.". Der Platzhalter „52.2691/9.1342" nutzt `colorTextPlaceholder` = `schwach` (LFH-618: Tag 6,37 < 7) | LFH-650 · LFH-643 |
| 6 | **Kein Status allein über Farbe** — jede Statusfarbe zusätzlich mit Text, Symbol oder Form, 0 Ausnahmen (WCAG 1.4.1 Level A; MIL 5.4.6.8; 1 von 12 Männern, NEI). | **erfüllt** | Jede Feldablehnung ist ein Satz am Feld: `PersonErfassungModal.test.tsx` „sendet eine unbrauchbare Koordinate NICHT und sagt warum"; `PersonenDetailPage.test.tsx` „hält eine unbrauchbare Koordinate an, statt zu senden". Ein fehlender Wert ist „—", kein Grauton: „zeigt Zustand und Koordinate im Lesemodus; der Verorten-Link trägt den Platzier-Auftrag" | — |
| 7 | **Eine Farbe = eine Bedeutung** — Palette auf Doppelbelegung geprüft, gesättigte Farbe nur für abnorme Zustände, Grundfläche aus A0 (weder `#000000` noch `#ffffff`) — ASM Consortium. | **erfüllt [abgeleitet]** | Keine neue Farbe, kein `danger` (Grep `e4665ed3`); der Verorten-Link steht in der Bedienrolle, Rot nur für die antd-Feldmeldung | — |
| 8 | **Helligkeits-/Kontrastregler** vorhanden und bei aktiver Warnung nicht bis AUS dimmbar — 1 Regler, 1 Sperre (MIL 5.2.2.1.9, 5.2.4.2.2.3). | **offen** | App-weite Lücke, kein Regler | LFH-397 |
| 9 | **Kritische Anzeigen im Blickfeld** — innerhalb 15° der normalen Blickachse, nicht am Layoutrand (MIL 5.2.2.1.7). | **erfüllt** | Ablehnungen stehen am Feld, und die Maske bleibt offen: `PersonErfassungModal.test.tsx` „sendet eine unbrauchbare Koordinate NICHT und sagt warum", „lässt den Wortlaut stehen, wenn das Speichern fehlschlägt"; Detailseite: „hält eine unbrauchbare Koordinate an, statt zu senden". Die Zukunftsgrenze ist clientseitig gespiegelt (fünf Minuten wie im Backend, `LagedatenFelder.tsx`), ein 400 erreicht den Server also nicht erst | — |
| 10 | **Alarmbudget eingehalten** — 1–2 je 10 min im Dauerbetrieb, < 10 in den ersten 10 min einer Großlage, ≤ 10 je 10-min-Fenster, ~80/15/5 %, 0 flatternde Alarme, ≤ 3 Eskalationsstufen (EEMUA 191 S. 96/97; ISA-18.2). | **nicht anwendbar** | Keine ungefragte Meldung: neue Toasts gibt es auf dieser Fläche nicht (Grep); „Objekt verortet" auf der Lagekarte ist Bestand und folgt einer Nutzeraktion | — |
| 11 | **Warnverhalten** — kein Blinken auf lesbarem Text, ≤ 2 Blinkraten (schnellere ≤ 5 Hz, langsamere ≥ 0,8 Hz), jede Warnung quittierbar, jeder Ton mit visueller Entsprechung (MIL 5.2.1.5.5.3/.4/.5, 5.3.6.3). | **erfüllt [abgeleitet]** | Kein Blinken, kein Ton (Grep 0) | — |
| 12 | **Kein Sprung unter dem Cursor** — CLS ≤ 0,1 (75. Perzentil, web.dev); neue Datensätze nur als opt-in-Sammelbanner (WCAG 3.2.5 / G76). | **erfüllt [abgeleitet]** | Das sichtbare Budget der Maske wächst nicht, die neuen Felder erscheinen erst nach ausdrücklichem Aufklappen: `AufnahmeFelder.test.tsx` „%s: sichtbares Budget unverändert (%i), die neuen Felder erst nach dem Aufklappen" (schnell 4, betroffen 4, vermisst 3). Die Bearbeiten-Sitzung der Detailseite friert ihre Werte beim Öffnen ein (`starteBearbeiten` → `bearbeitenWerteAus`, Bestand `useEditSitzung`), ein Live-Stand ändert das offene Formular nicht; eine unveränderte Koordinate geht gar nicht mit: `PersonenDetailPage.test.tsx` „lässt eine unveränderte Koordinate weg — keine stille Rundung auf vier Stellen" | — |
| 13 | **Fokus nie verdeckt** — 0 vollständig verdeckte Fokusziele beim Tab-Durchlauf hinter fixierten Köpfen, Fußleisten oder Drawern (WCAG 2.4.11 AA). | **erfüllt [abgeleitet]** | Keine fixierte Konstruktion in Maske, Aufnahme-Route oder Detailseite (Grep `sticky`/`fixed` = 0). Kein Tab-Durchlauf gemessen | — |
| 14 | **Tabellenseite vollständig** — fixierte Kopfzeile, fixierte menschenlesbare Identifierspalte, umschaltbarer Spaltensatz mit Zähler ausgeblendeter Spalten, keine Auflösung in Karten, wo verglichen wird (NN/g Data Tables / Mobile Tables). | **nicht anwendbar** | Maske und Einzeldatensatz, keine Menge | — |
| 15 | **Erfassungsmaske vollständig** — Defaults vorbelegt, sichtbar und einzeln überschreibbar (MIL 5.14.7.1/.3), „Speichern und nächsten anlegen" mit gehaltenem Kontext (5.14.7.4), Sammelliste mit Ändern/Entfernen je Zeile (DWP „Add another thing"), Labels über dem Feld (50 ms statt 500 ms Sakkade, Penzo), volle Tastaturbedienung (WCAG 2.1.1). | **erfüllt** | Hülle und Serienmodus unverändert (Erfassungs-Norm LFH-332). Neu: der Default von „vermisst seit" ist **sichtbar gesagt** („Ohne Angabe gilt der Zeitpunkt der Meldung.") und geht ohne Angabe gar nicht mit: `AufnahmeFelder.test.tsx` „schickt ohne Angabe weder Koordinate noch „vermisst seit" — auch nicht als null". Die Koordinate ist **ein** Textfeld statt zweier Zahlenfelder: „zerlegt die Koordinate in antreff_lat/antreff_lon und lässt den Text weg"; beide Mounts senden sie mit: `PersonErfassungModal.test.tsx` „schickt Zustand und die zerlegte Koordinate mit — den Koordinatentext nicht", `AufnahmePage.test.tsx` „schickt Zustand und Koordinate aus „Weitere Angaben" im selben POST (LFH-613)". Zeitpunkt per Tastatur getippt, Enter sendet: „Vermisst-Modus: „vermisst seit" geht als UTC-Wire-String mit". Felder passend zum Status: „vermisst: kein Zustand, keine Koordinate, kein Verorten — ein Fundort gibt es nicht". Labels über dem Feld (`layout="vertical"` in Hülle und Detailformular). Der Verorten-Weg braucht einen Mausklick auf die Karte; den **Tastaturweg** zur selben Angabe trägt das Koordinatenfeld | — |

**Verdikt-Bilanz:** 8 erfüllt · 4 offen · 3 nicht anwendbar.

---

## Tabelle 4 — Kartenansicht der Betroffenen (`personen/BetroffeneKarte.tsx`)

| Nr. | Kriterium | Verdikt | Beleg / Begründung | Zielticket |
| --- | --- | --- | --- | --- |
| 1 | **Treffläche** — Boden ≥ 24 × 24 CSS px oder 24-px-Umkreis frei (WCAG 2.5.8 AA); zeitkritische Aktion ≥ 48 × 48 px mit ≥ 8 px Abstand (WCAG 2.5.5 / Material 48 dp). | **offen** | Nicht gemessen. **[abgeleitet]:** Personen-Marker tragen kein taktisches Zeichen (`personenMarker` setzt kein `tz`), sie zeichnen also über `marker-kreis`: `circle-radius: 9` plus `circle-stroke-width: 2` (`markerLayer.ts`, `KREIS_PAINT`) ergibt 18 px Füllung und rund 22 px mit Rand, **unter** dem 24-px-Boden. Die Plakette ist ebenfalls Klickziel (`MARKER_KLICK_LAYER`), erscheint aber erst ab Zoom 12 (Nr. 6). Für einzeln stehende Marker kann die Umkreis-Ausnahme von WCAG 2.5.8 tragen, für benachbarte nicht. Ungemessen sind auch die Zoom-/Nord-Knöpfe der `KartenUeberlagerung` auf dieser Route | LFH-650 |
| 2 | **Handschuh-Modus** vorhanden und geprüft — Zeilenhöhe ≥ 72 px (= 19,05 mm [abgeleitet], MIL-STD-1472F Fig. 12), Abstand ≥ 16 px [abgeleitet aus Fig. 24, last contact]. | **offen** | Die Markergröße ist eine feste MapLibre-Angabe und folgt der Dichtestufe **nicht** [abgeleitet] — in `handschuh` bleibt es bei rund 22 px. Wahrscheinlicher Befund | LFH-650 |
| 3 | **Rückmeldung vor der Serverantwort** — sichtbar ≤ 100 ms (MIL 5.4.6.4); Kommandoreaktion ≤ 2 s (Tab. XXII); > 15 s nur mit Fortschrittsmeldung (MIL 5.14.9). | **erfüllt [abgeleitet]** | Das nachgeladene Bündel und der Personen-Abruf zeigen ein Skeleton statt einer leeren Karte (`Suspense fallback={<SeitenSkeleton />}` und `personenQuery.isLoading` → `SeitenSkeleton`, `PersonenPage.tsx`); ein gescheiterter Abruf läuft über den gemeinsamen Fehlerzweig **vor** der Ansichtsweiche (`listeGescheitert` → `SeitenFehler`), belegt für die Seite in `PersonenPage.test.tsx` „zeigt bei gescheitertem Abruf den Fehler und NICHT die Leertexte". Leer ist nicht Fehler: „ohne Koordinate und ohne Einsatzort: Leerzustand statt einer leeren Karte" | — |
| 4 | **Kritische Aktion hat eine zweite Handlung** — Storno, Abschluss, Löschen, Alarmierung: je 1 zusätzliche Bestätigung (MIL 5.4.6.6). | **nicht anwendbar** | Die Karte schreibt nicht (Dateikopf `BetroffeneKarte.tsx`: keine Zeichen-, Zonen- oder Fachebenen-Props); der Markerklick navigiert: `PersonenPage.test.tsx` „der Markerklick führt zur Detailseite der Person" | — |
| 5 | **Kontrast in beiden Modi** — Text Tag ≥ 7 : 1, Nacht ≥ 5 : 1, nie < 4,5 : 1 (MIL 5.2.4.2.2.2; WCAG 1.4.6/1.4.3); Zustände, Rahmen, Fokusring ≥ 3 : 1 (WCAG 1.4.11). | **offen** | Ungemessen: Sichtungsfarben der Marker gegen die Grundkarte (SK II gelb auf heller, „tot" schwarz auf dunkler Grundlage; der weiße 2-px-Rand ist die einzige Kante), Plakette „R-042 · SK II", Hinweis „n Personen ohne Koordinate" (`colorTextSecondary`, `fontSizeSM`) und der Leerzustand | LFH-650 |
| 6 | **Kein Status allein über Farbe** — jede Statusfarbe zusätzlich mit Text, Symbol oder Form, 0 Ausnahmen (WCAG 1.4.1 Level A; MIL 5.4.6.8; 1 von 12 Männern, NEI). | **offen** | **Trägt:** das Marker-Objekt führt den zweiten Kanal, auch für die Abwesenheit einer Sichtung: `personenKarte.test.ts` „ohne Sichtung: neutrale Farbe, und die Beschriftung SAGT es (zweiter Kanal)", „unverletzt hat keine Fachfarbe und fällt auf neutral, Label nennt die Kategorie". **Nachgezogen nach dem ersten Entwurf dieser Liste:** die Plakette hängt weiter an Zoom 12 und darf bei Kollision weichen — deshalb trägt jeder Einzelmarker jetzt ein **Kurzzeichen IM Kreis** (`I`/`II`/`III`/`IV`/`T`/`U`/`–`), ohne Mindestzoom und ohne Kollision (`marker-kurz`/`spider-kurz`): `markerLayer.test.ts` „zeigt es OHNE Mindestzoom und ohne Kollision — anders als die Plakette", `personenKarte.test.ts` (Spec-Szenario, `kurzzeichen: 'II'`). **Weiter offen:** gezeichnet ist das nicht gemessen (WebGL), und **Cluster** (bis Zoom 14) zeigen die Anzahl und die Objektart, nicht die Kategorie — wer im Cluster nach SK I sucht, muss auffächern | LFH-650 |
| 7 | **Eine Farbe = eine Bedeutung** — Palette auf Doppelbelegung geprüft, gesättigte Farbe nur für abnorme Zustände, Grundfläche aus A0 (weder `#000000` noch `#ffffff`) — ASM Consortium. | **offen** | **Trägt:** Markerfarben kommen ausschließlich aus der Sichtungsachse (`tokens.ts`, `sichtungsfarben`): `personenKarte.test.ts` „SK I rot, tot schwarz — die Farben kommen aus der Sichtungsachse". **Offen:** das Cluster-Segment `person` ist ein neues Literal `#be185d` (Rosé, `clusterDonut.ts`) außerhalb der Tokens. Es steht auf derselben Karte neben SK-I-Rot und ist nicht auf Verwechslung geprüft; ein rosé Donut kann als „SK I" gelesen werden | LFH-650 |
| 8 | **Helligkeits-/Kontrastregler** vorhanden und bei aktiver Warnung nicht bis AUS dimmbar — 1 Regler, 1 Sperre (MIL 5.2.2.1.9, 5.2.4.2.2.3). | **offen** | App-weite Lücke, kein Regler | LFH-397 |
| 9 | **Kritische Anzeigen im Blickfeld** — innerhalb 15° der normalen Blickachse, nicht am Layoutrand (MIL 5.2.2.1.7). | **offen** | Die kritische Aussage dieser Fläche ist die Lücke: „n Personen ohne Koordinate — nicht auf der Karte". Sonst läse sich die Karte als vollständig. Sie steht als erstes Element über der Karte (`BetroffeneKarte.tsx`) **[abgeleitet]**; die e2e-Spec prüft ihren Wortlaut, nicht ihre Lage. Ob sie bei 1366 × 768 unter Seitenkopf, Filterleiste und Segmentleiste im ersten Bild steht, ist nicht gemessen | LFH-650 |
| 10 | **Alarmbudget eingehalten** — 1–2 je 10 min im Dauerbetrieb, < 10 in den ersten 10 min einer Großlage, ≤ 10 je 10-min-Fenster, ~80/15/5 %, 0 flatternde Alarme, ≤ 3 Eskalationsstufen (EEMUA 191 S. 96/97; ISA-18.2). | **nicht anwendbar** | Keine Meldung, kein Toast, kein Ton (Grep über `BetroffeneKarte.tsx`/`personenKarte.ts` = 0) | — |
| 11 | **Warnverhalten** — kein Blinken auf lesbarem Text, ≤ 2 Blinkraten (schnellere ≤ 5 Hz, langsamere ≥ 0,8 Hz), jede Warnung quittierbar, jeder Ton mit visueller Entsprechung (MIL 5.2.1.5.5.3/.4/.5, 5.3.6.3). | **erfüllt [abgeleitet]** | Kein Blinken, keine Animation (Grep 0); der Startausschnitt springt ohne Flug (`fitBounds … duration: 0`, `Kartenflaeche.tsx`) | — |
| 12 | **Kein Sprung unter dem Cursor** — CLS ≤ 0,1 (75. Perzentil, web.dev); neue Datensätze nur als opt-in-Sammelbanner (WCAG 3.2.5 / G76). | **offen** | **Trägt [abgeleitet]:** der Startausschnitt wird je Karte genau einmal angewendet (`startAufKarteRef`, `Kartenflaeche.tsx:604`), neue Personen verschieben den Ausschnitt nicht. **Offen [abgeleitet]:** (a) der Hinweis über der Karte wird nur bei `ohneKoordinate > 0` gerendert. Wird die letzte Person ohne Koordinate live verortet oder kommt die erste hinzu, springt die ganze Karte um eine Zeile unter dem Zeiger. (b) Eine neue Person nahe einem bestehenden Marker lässt beide zu einem Cluster verschmelzen, der Marker unter dem Zeiger verschwindet. Kein Sammelbanner | LFH-650 |
| 13 | **Fokus nie verdeckt** — 0 vollständig verdeckte Fokusziele beim Tab-Durchlauf hinter fixierten Köpfen, Fußleisten oder Drawern (WCAG 2.4.11 AA). | **offen** | Die Karte bindet dieselbe `KartenUeberlagerung` ein wie die Lagekarte (`position: 'absolute'`, `zIndex: 5`, eigene Tab-Stopps). Die Lagekarten-Prüfliste (2026-07-30, Nr. 13) führt genau diese Aufbauten offen | LFH-373 |
| 14 | **Tabellenseite vollständig** — fixierte Kopfzeile, fixierte menschenlesbare Identifierspalte, umschaltbarer Spaltensatz mit Zähler ausgeblendeter Spalten, keine Auflösung in Karten, wo verglichen wird (NN/g Data Tables / Mobile Tables). | **nicht anwendbar** | Geo-Ansicht **zusätzlich** zur Tabelle, über die Segmentleiste gewählt, Vorgabe bleibt „Zeilen": `PersonenPage.test.tsx` „?ansicht=karte öffnet die Kartenansicht und räumt den Parameter"; Filter gelten gleich: „der Statusfilter gilt auch auf der Karte" | — |
| 15 | **Erfassungsmaske vollständig** — Defaults vorbelegt, sichtbar und einzeln überschreibbar (MIL 5.14.7.1/.3), „Speichern und nächsten anlegen" mit gehaltenem Kontext (5.14.7.4), Sammelliste mit Ändern/Entfernen je Zeile (DWP „Add another thing"), Labels über dem Feld (50 ms statt 500 ms Sakkade, Penzo), volle Tastaturbedienung (WCAG 2.1.1). | **nicht anwendbar** | Keine Erfassung; verortet wird über die Detailseite (Tabelle 3) | — |

**Verdikt-Bilanz:** 2 erfüllt · 9 offen · 4 nicht anwendbar.

---

## Tabelle 5 — Lage-Dashboard: „Transportiert / offen" und „n seit über 4 h"

| Nr. | Kriterium | Verdikt | Beleg / Begründung | Zielticket |
| --- | --- | --- | --- | --- |
| 1 | **Treffläche** — Boden ≥ 24 × 24 CSS px oder 24-px-Umkreis frei (WCAG 2.5.8 AA); zeitkritische Aktion ≥ 48 × 48 px mit ≥ 8 px Abstand (WCAG 2.5.5 / Material 48 dp). | **nicht anwendbar** | Kein neues Bedienziel: die Fußzeile ist Text im Paneel (`LagePaneele.tsx`, `data-lfh="transport-bilanz"`), die Notiz Text in der bestehenden Kennzahl-Zelle „Vermisste". Die Zelle selbst ist Bestand und nicht Gegenstand dieser Liste | — |
| 2 | **Handschuh-Modus** vorhanden und geprüft — Zeilenhöhe ≥ 72 px (= 19,05 mm [abgeleitet], MIL-STD-1472F Fig. 12), Abstand ≥ 16 px [abgeleitet aus Fig. 24, last contact]. | **nicht anwendbar** | Wie Nr. 1 | — |
| 3 | **Rückmeldung vor der Serverantwort** — sichtbar ≤ 100 ms (MIL 5.4.6.4); Kommandoreaktion ≤ 2 s (Tab. XXII); > 15 s nur mit Fortschrittsmeldung (MIL 5.14.9). | **erfüllt** | Der Fuß steht nur im Zustand `daten` (`LagePaneele.tsx`), er behauptet also vor den Daten und nach einem Fehler keine Zählung: `LageDashboardPage.test.tsx` „bei gescheitertem Personen-Abruf zeigt es den Fehler und NICHT den Leertext", „Spec-Szenario „Transportiert / offen": 2 / 2 — eine Voranmeldung ist kein Transport". Die Notiz zieht mit dem Uhr-Takt nach, ohne neue Daten: „die Notiz erscheint nach Ablauf der Schwelle OHNE neue Daten" | — |
| 4 | **Kritische Aktion hat eine zweite Handlung** — Storno, Abschluss, Löschen, Alarmierung: je 1 zusätzliche Bestätigung (MIL 5.4.6.6). | **nicht anwendbar** | Reine Anzeige | — |
| 5 | **Kontrast in beiden Modi** — Text Tag ≥ 7 : 1, Nacht ≥ 5 : 1, nie < 4,5 : 1 (MIL 5.2.4.2.2.2; WCAG 1.4.6/1.4.3); Zustände, Rahmen, Fokusring ≥ 3 : 1 (WCAG 1.4.11). | **offen** | **Trägt [abgeleitet]:** die Notiz ist dasselbe Primitiv `Kennzahl` in derselben Rolle `gedaempft`, die LFH-606 [M3] gemessen hat (Tag 8,42, Nacht 7,27). **Offen:** das Etikett „Transportiert / offen" steht in `schwach`, 11 px, auf dem Paneel. LFH-618 hat dafür am Tag 5,84 gemessen, unter 7:1. Der Wert daneben (`text2`, Mono 12 px) ist nicht gemessen | LFH-643 · LFH-650 |
| 6 | **Kein Status allein über Farbe** — jede Statusfarbe zusätzlich mit Text, Symbol oder Form, 0 Ausnahmen (WCAG 1.4.1 Level A; MIL 5.4.6.8; 1 von 12 Männern, NEI). | **erfüllt** | Beide Angaben sind Wörter und Zahlen: `LageDashboardPage.test.tsx` „Spec-Szenario: drei Vermisste, zwei seit über 4 h → Notiz „2 seit über 4 h""; `lagebild.test.ts` „vermisstNotiz: ohne Vermisste der Leerwortlaut"; die Alarmkante an „Vermisste" behält ihr Wort: „ohne Vermisste gibt es keine Kante und den Wortlaut statt einer nackten Null" | — |
| 7 | **Eine Farbe = eine Bedeutung** — Palette auf Doppelbelegung geprüft, gesättigte Farbe nur für abnorme Zustände, Grundfläche aus A0 (weder `#000000` noch `#ffffff`) — ASM Consortium. | **erfüllt [abgeleitet]** | Keine neue Farbe: der Fuß nutzt Textrollen, und der `ton` von „Vermisste" hängt weiter nur an `vermisst > 0` (`lagebild.ts`, Zeile unverändert), die 4-h-Notiz färbt nichts; Farbliterale 0 (Grep) | — |
| 8 | **Helligkeits-/Kontrastregler** vorhanden und bei aktiver Warnung nicht bis AUS dimmbar — 1 Regler, 1 Sperre (MIL 5.2.2.1.9, 5.2.4.2.2.3). | **offen** | App-weite Lücke; „Vermisste" mit Alarmkante ist eine aktive Warnung, die Sperre fehlt ebenso | LFH-397 |
| 9 | **Kritische Anzeigen im Blickfeld** — innerhalb 15° der normalen Blickachse, nicht am Layoutrand (MIL 5.2.2.1.7). | **erfüllt** | Die Notiz steht **in** der Kennzahl „Vermisste" im Band „Lage in Zahlen", dessen Zellenfolge fest ist: `LageDashboardPage.test.tsx` „trägt genau 6 Kennzahlen in fester Reihenfolge" (dieselbe Belegart wie LFH-606 [T3]); der Transport-Fuß ist keine kritische Anzeige | — |
| 10 | **Alarmbudget eingehalten** — 1–2 je 10 min im Dauerbetrieb, < 10 in den ersten 10 min einer Großlage, ≤ 10 je 10-min-Fenster, ~80/15/5 %, 0 flatternde Alarme, ≤ 3 Eskalationsstufen (EEMUA 191 S. 96/97; ISA-18.2). | **erfüllt [abgeleitet]** | Die 4-h-Schwelle ist **keine** zusätzliche Eskalationsstufe: sie ändert nur den Wortlaut, nicht den `ton` (Nr. 7), und erzeugt weder Toast noch Ton (Grep). Die Schwelle ist scharf und flattert nicht: `lagebild.test.ts` „zählt genau 4 h NICHT, eine Sekunde darüber schon", „pinnt die Schwelle als Literal (Neuentwurf S3)" | — |
| 11 | **Warnverhalten** — kein Blinken auf lesbarem Text, ≤ 2 Blinkraten (schnellere ≤ 5 Hz, langsamere ≥ 0,8 Hz), jede Warnung quittierbar, jeder Ton mit visueller Entsprechung (MIL 5.2.1.5.5.3/.4/.5, 5.3.6.3). | **erfüllt [abgeleitet]** | Kein Blinken (Grep 0); die Notiz wechselt im 5-s-Takt (`TAKT_MS`) höchstens ihren Wortlaut, ohne Bewegung | — |
| 12 | **Kein Sprung unter dem Cursor** — CLS ≤ 0,1 (75. Perzentil, web.dev); neue Datensätze nur als opt-in-Sammelbanner (WCAG 3.2.5 / G76). | **offen** | Der Fuß des Sichtungspaneels erscheint erst mit den Daten, eine bis zwei Zeilen hoch. Vorher gab es ihn nur bei „Ohne Sichtung > 0", jetzt immer. Die Zeile „Ohne Sichtung" kommt und geht weiter live und schiebt jetzt die Transport-Zeile mit. Die CLS des Dashboards ist nur für die Pegel-Angabe gemessen (LFH-606 [M5]), nicht für diesen Fuß | LFH-650 |
| 13 | **Fokus nie verdeckt** — 0 vollständig verdeckte Fokusziele beim Tab-Durchlauf hinter fixierten Köpfen, Fußleisten oder Drawern (WCAG 2.4.11 AA). | **erfüllt [abgeleitet]** | Keine fixierte Konstruktion in `pages/lage-dashboard/` (Grep `sticky`/`fixed` = 0), kein neuer Tab-Stopp | — |
| 14 | **Tabellenseite vollständig** — fixierte Kopfzeile, fixierte menschenlesbare Identifierspalte, umschaltbarer Spaltensatz mit Zähler ausgeblendeter Spalten, keine Auflösung in Karten, wo verglichen wird (NN/g Data Tables / Mobile Tables). | **nicht anwendbar** | Überblicksfläche, keine Tabelle | — |
| 15 | **Erfassungsmaske vollständig** — Defaults vorbelegt, sichtbar und einzeln überschreibbar (MIL 5.14.7.1/.3), „Speichern und nächsten anlegen" mit gehaltenem Kontext (5.14.7.4), Sammelliste mit Ändern/Entfernen je Zeile (DWP „Add another thing"), Labels über dem Feld (50 ms statt 500 ms Sakkade, Penzo), volle Tastaturbedienung (WCAG 2.1.1). | **nicht anwendbar** | Keine Erfassung | — |

**Verdikt-Bilanz:** 7 erfüllt · 3 offen · 5 nicht anwendbar.

---

## Gesamtbilanz

| Fläche | erfüllt | offen | nicht anwendbar |
| --- | --- | --- | --- |
| 1 · Betroffenenliste (Zustand, Fundort-Koordinate) | 4 | 8 | 3 |
| 2 · Schnellerfassungszeile `#lat/lon` | 7 | 3 | 5 |
| 3 · Maske „Weitere Angaben" + Detailseite | 8 | 4 | 3 |
| 4 · Kartenansicht | 2 | 9 | 4 |
| 5 · Lage-Dashboard | 7 | 3 | 5 |
| **Summe (75 Zeilen)** | **28** | **27** | **20** |

Von den 27 offenen Zeilen gehören 5 zum app-weiten Regler (LFH-397) und 2 zur
Fokusverdeckung hinter fixierten Aufbauten (LFH-373). LFH-643 (`schwach` unter dem Tagesboden)
trägt Teile von drei Zeilen (1·5, 3·5, 5·5). Der Rest ist der Nachzug unten.

## Nachzüge

Zielticket für alles unten: **LFH-650**. Die
Messungen gehören nach dem Muster von LFH-46/LFH-606 in `e2e/gate3-trefflaeche.spec.ts` (Böden als
Literale, Mengen über `alleHaltenStufe`, Mutationsprobe) bzw. eine Kontrast-Spec mit
`e2e/kontrast-kern.ts`, Fükw 1366 × 768, wo nicht anders genannt.

| Fläche · Nr. | Route | Element | Was gemessen oder gebaut werden muss |
| --- | --- | --- | --- |
| 1 · 1, 1 · 2 | `/einsaetze/:id/personen` (Zeilen) | „Zustand hinzufügen" (`BemerkungZelle`, leer) und der Stift der gefüllten Zelle | Höhe gegen 30 / 48 / 72 in `kompakt` / `komfortabel` / `handschuh`, Breite ≥ 24 px; Abstand zum Nachbarziel ≥ 16 px in `handschuh`. Der Stift hat keine Ableitung: Mindestwert je Stufe erheben |
| 1 · 3, 1 · 9 | dieselbe | Zustand-Zelle nach Speichern | Umbau statt Messung: sichtbarer Laufzustand (oder optimistischer Wert) statt eines zurückkehrenden „Zustand hinzufügen"; Fehler **an der Zeile** (`data-fehler`/Rand nach LFH-345 · H15) statt nur `message.error` |
| 1 · 5 | dieselbe, Tag und Nacht | Zustand-Knopf (Bedienrolle), Zustandstext (`text2`, 12 px), Koordinate (`gedaempft`, Mono 11 px) in der Fundort-Spalte | Tag ≥ 7 : 1, Nacht ≥ 5 : 1; der Strich „—" gehört zu LFH-643 |
| 1 · 12 | dieselbe, `handschuh` | Zeile vor und nach dem ersten Speichern eines Zustands | Höhenänderung der Zeile (Knopf 72 px → Text) und CLS der Folgezeilen; ebenso eine live eintreffende Koordinate (Fundort-Zelle wird zweizeilig) bei offener Liste |
| 2 · 5 | `/einsaetze/:id/personen`, Tag und Nacht | Marke `#52.2691/9.1342` in „erkannt: …" (`gedaempft`, Mono 12 px) | Kontrast wie oben |
| 2 · 12 | dieselbe, 390 px | Hinweiszeile beim Tippen von `Name sk2 @UHS #lat/lon` | Umbruch der Zeile und Verschiebung der Liste darunter (CLS) |
| 3 · 1, 3 · 2 | `/einsaetze/:id/personen/:personId` | „Auf Lagekarte verorten" (`verortenLinkStil`) | Gerenderte Höhe gegen 30 / 48 / 72 und Breite ≥ 24 px (LFH-396: ein Anker kann trotz Stil auf Zeilenhöhe bleiben) |
| 3 · 1, 3 · 2 | `/einsaetze/:id/personen/aufnahme` und Modal „Schnellerfassung", aufgeklappt | Felder „Zustand", „Koordinate", „vermisst seit" | Höhe in `handschuh` (72 px); belegt die bisher nur abgeleitete Aussage |
| 3 · 5 | Detailseite und Aufnahme, Tag und Nacht | Verorten-Link, Koordinate im Lesemodus, Feldmeldung „Liegt in der Zukunft" / Koordinatengrund, `extra`-Text von „vermisst seit" | Kontrast; der Platzhalter gehört zu LFH-643 |
| 4 · 1, 4 · 2 | `/einsaetze/:id/personen?ansicht=karte` | Marker-Kreis (rechnerisch ~22 px), Plakette ab Zoom 12, Cluster-Donut, Zoom-/Nord-Knöpfe der Überlagerung | Trefffläche in allen drei Stufen; Marker folgen der Stufe heute gar nicht. Entscheidung, ob der Kreis wächst oder die Umkreis-Ausnahme ausdrücklich getragen wird |
| 4 · 5 | dieselbe, Tag und Nacht, beide Grundkarten | Marker SK I–IV / tot / neutral gegen die Grundkarte (Kante ≥ 3 : 1), Plakette, Hinweis „n ohne Koordinate", Leerzustand | Kontrast; gelb auf heller und schwarz auf dunkler Grundlage sind die Kandidaten |
| 4 · 6 | dieselbe | Kurzzeichen im Kreis bei realistisch gestreuten Fundorten (einige km), Tag und Nacht | Im Browser sichtbar und lesbar (9 px, schwarzer Text mit weißem Hof); Entscheidung, ob ein Cluster die dringlichste Kategorie zeigen soll |
| 4 · 7 | dieselbe | Cluster-Segment `person` `#be185d` neben SK-I-Rot | Doppelbelegung prüfen; das Literal in die Tokens heben oder eine Nicht-Rot-Farbe wählen |
| 4 · 9 | dieselbe, 1366 × 768 mit offenem Modul-Panel | Hinweis „n Personen ohne Koordinate" | Lage im ersten Bild (`toBeInViewport`, nicht `toBeVisible`) |
| 4 · 12 | dieselbe | Hinweis kommt/geht live; Cluster-Verschmelzen bei neuer Person | Verschiebung der Karte messen; Abhilfe z. B. reservierter Platz für den Hinweis (auch „alle verortet" sagen) |
| 5 · 5 | `/einsaetze/:id/lage-dashboard`, Tag und Nacht | Wert „t / o" des Transport-Fußes (`text2`, Mono 12 px) | Kontrast; das Etikett in `schwach` gehört zu LFH-643 |
| 5 · 12 | dieselbe | Sichtungspaneel beim ersten Laden und beim Wechsel „Ohne Sichtung" 0 ↔ > 0 | CLS nach dem Muster von LFH-606 [M5] |

**App-weit, nicht in den Nachzug:** Regler (Nr. 8 aller Flächen) → **LFH-397**;
Fokusverdeckung an der stehenden Tabellenkopfzeile und an der `KartenUeberlagerung`
(1 · 13, 4 · 13) → **LFH-373**; `schwach` unter dem Tagesboden (1 · 5 „—", 3 · 5 Platzhalter,
5 · 5 Etikett) → **LFH-643**.

Fällt im Gesamt-Gate (Aufgabe 7.3) ein hier zitierter Test, wird seine Zeile auf **offen**
gesetzt, nicht still umgeschrieben.
