[package]
name = "karten-katalog"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
# Robustes URL-Parsing für die Loopback-Prüfung (schon im Workspace-Lock, transitiv via reqwest).
url = "2"
