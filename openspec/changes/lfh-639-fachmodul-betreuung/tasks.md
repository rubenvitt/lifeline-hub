# Tasks

Jede Aufgabe entsteht test-first (`superpowers:test-driven-development`): erst der rote
Test, dann der Code. Referenz für die Berührpunkte eines neuen Moduls sind die Commits von
LFH-635 (Ablösung) und LFH-632 (Dokumente):
`git log origin/alpha --grep 'LFH-635\|LFH-632' --name-only`.

## 1. Backend: Schema und Fachkern

- [x] 1.1 Migration `0117_betreuung.sql` mit den vier Tabellen aus design.md D1 (CHECKs, partielle UNIQUE-Indizes auf `(einsatz_id, bezeichnung) WHERE storniert_at IS NULL`, Index `(bezirk_id, zeitpunkt_at)` bzw. `(stelle_id, zeitpunkt_at)`). Unmittelbar vorher `ls migrations` und `git ls-tree origin/alpha migrations/` auf die höchste Nummer prüfen. Verifiziert durch den Migrationslauf in `cargo test --lib db` und einen Repo-Test, der eine doppelte Bezeichnung am UNIQUE scheitern sieht und nach dem Stornieren wieder zulässt
- [x] 1.2 `src/betreuung/mod.rs`: Enums `Erhebung`, `Raeumungszustand`, `BetreuungsstelleArt`, `BetreuungsstelleStatus` (Wire + `as_str`/`parse`), Anzeige-DTOs `EvakuierungsbezirkAnzeige`, `BetreuungsstelleAnzeige`, `BetreuungUebersicht`, `BelegungKopfzahl` (optionale Felder mit `skip_serializing_if`) und reine Textfunktionen für die ETB-Sätze aus design.md D5. Verifiziert durch `cargo test --lib betreuung`: Parse-Rundreise je Enum, ETB-Texte je Vorgang, und dass kein Text Sammelstelle, Standort oder Notiz enthält
- [x] 1.3 `src/betreuung/repo.rs`, Bezirke: `uebersicht(einsatz)` (aktueller Stand per Zeiger-Join, Abschnittsname), `bezirk_anlegen_tx`, `bezirk_aendern_tx` (Leerlauf-Riegel, ETB-Typ je Feld nach D5), `bezirk_stornieren_tx`. Verifiziert durch Repo-Tests zu allen Szenarien von „Evakuierungsbezirk“, „Bezirk fortschreiben“ und „Bezirk stornieren“, einschließlich „unveränderte Werte → kein ETB-Eintrag“
- [x] 1.4 `repo`, Standreihe: `stand_melden_tx` (Zeile + ETB-Meldung mit Vorwert und `ereigniszeit` + Zeiger über die eine „aktuell“-Abfrage), `stand_zuruecknehmen_tx` (Kennzeichnung + Zeiger neu + Berichtigung mit `berichtigt_eintrag_id`), 409 bei storniertem Bezirk, 422 bei doppelter Rücknahme. Verifiziert durch Repo-Tests zu allen Szenarien von „Stand melden“ und „Standmeldung zurücknehmen“, darunter „nachgetragene ältere Meldung lässt den Stand stehen“ und Atomarität (ein Fehler schreibt weder Zeile noch ETB)
- [x] 1.5 `repo`, Stellen und Belegung: `stelle_anlegen_tx`, `stelle_aendern_tx` (Schließen nur bei Belegung 0 oder ohne Meldung → sonst 422; Wiederöffnen erlaubt), `stelle_stornieren_tx`, `belegung_melden_tx` (422 bei geschlossener, 409 bei stornierter Stelle), `belegung_zuruecknehmen_tx`, `kopfzahl(einsatz, t)`. Verifiziert durch Repo-Tests zu allen Szenarien von „Betreuungsstelle“, „Belegung melden“ und „Kopfzahl in Betreuung zu einem Zeitpunkt“ (Stichtag 13:30 aus dem Spec-Beispiel, Stelle ohne Meldung nicht als 0 summiert)

## 2. Backend: Routen, Gates, Live, Schwärzung

- [x] 2.1 `src/einsatz/modul.rs`: `betreuung` in `MODUL_KEYS` (28 → 29), `modul_marker!{ Betreuung => "betreuung" }`, `PFAD_KEY`-Eintrag `/api/einsaetze/{id}/betreuung`. Verifiziert durch die Unit-Tests in `modul.rs` (`marker_key_werte_stimmen`)
- [x] 2.2 `src/live/mod.rs`: `LiveEvent::Betreuung` (`ALLE` 29 → 30, `as_str`, Gate `&["betreuung"]` mit Kommentar nach der Füll-Regel, Gate-Pin) und `tests/enum_wire_kontrakt.rs` (LiveEvent + vier neue Enums, voll qualifizierte Pfade). Verifiziert durch `cargo test --test enum_wire_kontrakt` und `cargo test --lib live`
- [x] 2.3 `src/routes/betreuung.rs` + `routes/mod.rs` + `app.rs`: die zwölf Endpunkte aus design.md D6 mit `EinsatzLesezugriff/EinsatzSchreibzugriff<Betreuung>`, `JsonBody`, `PfadParam`, `write_retry!`, `zeit()`-Parsing (Zukunft > 60 s → 400), danach `live.publiziere` (ETB) und `LiveEvent::Betreuung` (nur IDs). Statuscode-Kommentar mit Verweis auf `src/error.rs` (D3). Verifiziert durch `cargo test --test json_extractor_guard --test path_extractor_guard`
- [x] 2.4 `tests/betreuung.rs`: Statuscode-Paare 400 (Plangröße 0, Anzahl −1, unbekannte Räumung, Zeitpunkt in der Zukunft, unlesbarer Zeitpunkt) / 404 (fremder Abschnitt, fremde Organisation) / 409 (storniert, doppelte Bezeichnung) / 422 (belegte Stelle schließen, geschlossene Stelle belegen, doppelte Rücknahme) / 403 (Beobachter, Modul ausgeblendet). Dazu ein Durchstich Bezirk anlegen → Stand 212 → Stand 480 → Rücknahme mit ETB-Treffern je Schritt und ein Test, dass der SSE-Strom das Ereignis `betreuung` nur an Leser mit Modulrecht liefert. Verifiziert durch `cargo test --test betreuung`
- [x] 2.5 Guards: `MODUL_GET_PFADE` in `tests/modul_override.rs`; nicht in `DEFERRED_MODULE` von `tests/einsatz_kontext_guard.rs`. Verifiziert durch `cargo test --test modul_override --test einsatz_kontext_guard` (grün bis auf den Drift-Test `backend_modul_keys_decken_frontend_registry`, der erst mit dem Registry-Eintrag aus 3.6 grün wird)
- [x] 2.6 Schwärzung in `schwaerzung_registry.rs`: vier `TabellenRegel`. `bezeichnung` bekommt `PlatzhalterMitId` (ein gemeinsamer Platzhalter scheiterte am partiellen UNIQUE), `sammelstelle`/`standort`/`notiz` bekommen `NullSetzen`, Zahlen/Enums/FKs/Zeiten bleiben (`retain`). OpenAPI in `api_doc.rs`, dann `scripts/check-typ-codegen.sh`, `openapi.json` und `types.generated.ts` mitcommitten. Verifiziert durch `cargo test --lib schwaerzung`, einen Schwärzungs-Durchstich zum Spec-Szenario „Einsatz schwärzen“, `cargo test --test openapi_spec_aktuell` und das grüne Codegen-Skript

## 3. Frontend: Unterbau

- [x] 3.1 `api/queryKeys.ts`: `EINSATZ_KEYS.betreuung`, `einsatzKeys.betreuung()` und `EINSATZ_STREAM_EVENTS.betreuung` stehen seit 2.x; offen sind `betreuungKopfzahl(t)` unter demselben Prefix und `EINSATZ_KEYS.betreuung` an `abschnitt`. Verifiziert durch `queryKeys.test.ts` (Literal-Pin), `queryKeys.guard.test.ts` und `api/liveEvent.contract.test.ts` (tsc)
- [x] 3.2 `api/betreuung.ts` (Client für alle Endpunkte) und `api/types.ts` (Re-Exporte, Request-DTOs mit Suffix `…Eingabe`/`…Patch`). Verifiziert durch `api/apiResponseTypen.guard.test.ts` und `tsc`
- [x] 3.3 `theme/statusFarben.ts`: Verträge `raeumungszustand` und `betreuungsstelleStatus` nach design.md D8 (kein `bedien`) (`ALLE_MAPS` 20 → 22), dazu die reine Funktion `auslastung(belegt, kapazitaet)` (keine Kapazität → keine Einstufung; ≥ 90 % `achtung`, ≥ 100 % `alarm`, Wort Pflicht). Verifiziert durch `statusFarben.test.ts` (Literal-Liste, Länge) und Grenzfall-Tests 89/90/100/101 %
- [x] 3.4 `betreuung/evakuierungKennzahl.ts` + `useEvakuierungKennzahl` nach design.md D9. Verifiziert durch Tests zu allen Szenarien der Kennzahl-Anforderung und das Paar „kein Bezirk → null“ / „Abruffehler → Zustand fehler“. Der Hook lädt nicht ohne `darfZaehlerLaden('betreuung', …)`
- [x] 3.5 `routing/deeplinks.ts`: `betreuungPfad(einsatzId, { bezirk?, stelle? })`. Verifiziert durch `deeplinks.test.ts` (Round-Trip durch `URLSearchParams`)
- [x] 3.6 `einsatz/modulRegistry.ts` (Kategorie Erfassung nach `unfallhilfsstellen`, Icon aus `react-icons/tb`, `status: 'fertig'`), `App.tsx` (`MODUL_ELEMENTE`) und `useModulZaehler.ts` (`berechneBetreuungZaehler`: Zahl der aktiven Bezirke, hinter `darfZaehlerLaden`). Verifiziert durch `modulRegistry.test.ts` und `sprungmarken.test.ts` (Reihenfolge-Pins), `useModulZaehler.test.ts` und den Backend-Drift-Test `backend_modul_keys_decken_frontend_registry`

## 4. Frontend: Seite

- [ ] 4.1 `pages/BetreuungPage.tsx` mit Block „Evakuierung“ (`Datensicht form="karte"`, Plan-Modus, „N · von M geplant“, „≈“ bei geschätzt, „keine Meldung“ statt 0, Dropdown-Bündelung mit Zeilenkennung) und Block „Betreuungsstellen“ (`Datensicht form="tabelle"`, Spalten nach D7, „frei“ nur mit Kapazität, Auslastungswort). Genau eine Primäraktion im Kopf. `RechteHinweis` und gesperrte Primäraktion ohne Schreibrecht. Deeplink `?bezirk=`/`?stelle=` scrollt auf die Zeile. Verifiziert durch `BetreuungPage.test.tsx`: Leerzustand, beide Blöcke mit Daten, genau ein Primärknopf in `[data-lfh="seitenkopf-aktionen"]`, Beobachter-Paar (Hinweis + gesperrt / Zeilenaktionen weg), Deeplink
- [ ] 4.2 `betreuung/BetreuungDialoge.tsx`: Anlegen Bezirk/Stelle, Stand melden, Belegung melden, Plangröße/Räumung/Stelle bearbeiten, alle über `ErfassungsModal` (≤ 3 sichtbare Felder, Rest eingeklappt, `mutateAsync`). Stornieren als `Modal` mit `danger`-Knopf. Schließen einer belegten Stelle bietet die Leermeldung im selben Dialog an. Zeitpunkte gehen über `alsBackendZeit` hinaus und kommen über dessen Umkehr aus `etb/filterZeit.ts` zurück, nie über `dayjs(s)`. Verifiziert durch Tests: gesendeter Wire-Wert gegen den absoluten Zeitpunkt, Feldbudget samt „Aufklappen → Zahl steigt“, Struktur „Knopf im `<form>`, kein `.ant-modal-footer`“, 422-Pfad lässt die Felder stehen
- [ ] 4.3 Rückgängig-Toast nach Stand- und Belegungsmeldung über `kommunikation/rueckgaengig.tsx` (fester Schlüssel). Verifiziert durch einen Test: „Rückgängig“ ruft die Rücknahme-Route mit der ID der gerade angelegten Meldung, eine zweite Meldung ersetzt den stehenden Toast
- [ ] 4.4 Lückenvermerke in `pages/lage-dashboard/lagebild.ts`, `LageDashboardPage.tsx`, `docs/design/2026-09-21-neuentwurf/umsetzung.md` und `CLAUDE.md` im Grund richtigstellen („Quelle seit LFH-639, Platz LFH-640, Einbau LFH-607“), ohne die Kennzahlreihe zu ändern. Verifiziert durch unveränderte `LageDashboardPage.test.tsx` (sechs Kennzahlen, Abwesenheitstest bleibt grün)

## 5. Nachweis und Abschluss

- [ ] 5.1 e2e: Route in `e2e/gate1-ueberlauf.spec.ts` (390/1024/1366 px), Gate-3-Test „Betreuung: Karten- und Zeilenaktionen folgen der Dichte-Staffel 30 / 48 / 72 px“ in `e2e/gate3-trefflaeche.spec.ts` mit gesäten Bezirken und Stellen (Fixture-Namen ohne „Betreuung“) und Mutationsprobe „Stufe festgenagelt → rot“. Verifiziert durch `pnpm e2e` auf beide Specs
- [ ] 5.2 Prüfliste `docs/superpowers/specs/2026-09-23-lfh-639-pruefliste.md` nach dem Muster von `2026-09-22-lfh-635-pruefliste.md`: alle 15 Kriterien mit Verdikt und Beleg, offene Punkte mit Zielticket. Verifiziert dadurch, dass keine Zeile „nicht geprüft“ trägt
- [ ] 5.3 Nachzüge als Tickets im Entwicklungsboard anlegen (`clickup-task-anlegen`): Lagekarte für Betreuung (Stellen-Marker, Bezirk als Fläche), FK `person_verbleib.betreuungsstelle_id`, Offline-Erfassung von Stand und Belegung, Verlaufsansicht der Meldereihen, Sammelbanner für fremd angelegte Bezirke und Stellen (Kriterium 12), und was die Prüfliste sonst offen lässt. Verifiziert durch die Ticketnummern in der Abschlussmeldung
- [ ] 5.4 Gesamtgate `./scripts/check-all.sh` grün (ohne `| tail`). Verifiziert durch den Exit-Code 0 des Laufs
