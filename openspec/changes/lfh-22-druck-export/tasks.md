# Tasks

Jede Aufgabe entsteht test-first (`superpowers:test-driven-development`): erst der rote Test,
dann der Code. Vor jeder „fertig“-Aussage gelten `verification-before-completion` und
`requesting-code-review`. Reihenfolge nach Auftrag: Druck im Fluss (LFH-71), Druckkopf,
Org-Branding-Backend, ETB-Druck.

## 1. Druck im normalen Fluss (LFH-71)

- [x] 1.1 `frontend/src/druck/druck.css` nach design.md D1 und D3 (Ausblenden per `display: none` über `:has`, Vorfahren-Neutralisierer, Wurzel im Fluss, Umbruchregeln `h1–h6`/`p, ul, ol, blockquote, pre, tr, img, figure`/`thead`, Papierfarben, `@page` mit statischem Zähler) und globaler Import. Verifiziert durch `druck/druck.test.ts` mit einem Parser, der Verschachtelung versteht: jede Regel ist an eine vorhandene Wurzel gebunden, kein `visibility`, `@page`-Inhalt ohne freien Text, alle Umbruchregeln vorhanden. Mutationsprobe: ausgeblendete Regel auf `visibility: hidden` zurück → rot.
- [x] 1.2 Die drei Seiten-Stylesheets umstellen (ein Commit mit ihren Tests): `visibility: hidden`, `position: absolute`, Farb- und doppelte Umbruchregeln in `lageberichtPrint.css`, `befehlPrint.css` und `kraefteuebersichtPrint.css` entfernen; Entwurfs-, `no-print`-, Seitenkopf- und Tabellenregeln bleiben. `data-lfh="druckwurzel"` an `.lagebericht-print-root`, `.befehl-print-root`, `.kraefte-print-root`. Kommentar `kraefteuebersichtPrint.css:86-89` berichtigen. Verifiziert durch die angepassten `lageberichtPrint.test.ts`/`befehlPrint.test.ts` (Gegenaussage: kein `visibility: hidden`, kein `position: absolute`; M86-Zusicherungen unverändert grün), einen neuen Quelltest für `kraefteuebersichtPrint.css` und `KraefteuebersichtPage.test.tsx`.
- [x] 1.3 Guard „genau eine Druckwurzel je Seite“: Seitentests von Lagebericht (Lese- und Entwurfszweig), Befehl und Meldebild zählen `[data-lfh="druckwurzel"]` = 1 und prüfen, dass Abschnittstitel und Editor-Vorschau im Entwurf innerhalb der Wurzel liegen. Verifiziert durch die Seitentests.
- [x] 1.4 e2e `frontend/e2e/druck-fluss.spec.ts` nach design.md D10: Lagebericht (Lese- und Entwurfszweig, Text über mehr als zwei Seiten, Endmarke im letzten Abschnitt) und Befehl, per API gesät; unter `emulateMedia({ media: 'print' })` Wurzel `position: static` und oben ≈ 0, Kopfleiste/Rail/Modulpanel/offene `message` `display: none`, Endmarke unterhalb einer A4-Höhe; `page.pdf` mit Seitenzahl im Band (Zähler mit Selbstprobe an einem einseitigen Dokument). Verifiziert durch grünen Lauf **und** rotes Ergebnis auf dem Stand vor 1.1/1.2 (Mutationsprobe im Spec-Kopf protokolliert).
- [x] 1.5 `e2e/meldebild-tabelle.spec.ts`: Kommentar zur `visibility`-Falle (`druckLage`) an die neue Mechanik anpassen und prüfen, dass die Neutralisierer-Nachweise unverändert greifen. Verifiziert durch grünen Spec-Lauf.

## 2. Gemeinsamer Druckkopf

- [x] 2.1 `components/druck/Druckkopf.tsx` nach design.md D2, zunächst **nur mit dem Namen** aus `GET /api/organisation`: Dokumentart/-titel als `h1`, Einsatz mit Einsatznummer, Zeilen für Stand/Auswahl, Ersteller, Druckzeitpunkt in der Org-Zone, `sichtbarkeit: 'druck' | 'immer'`. Verifiziert durch `Druckkopf.test.tsx`: alle Angaben stehen da, ohne Einsatznummer keine leere Klammer, Druckzeit über `useAnzeigeKonventionen` (Zone gegen absoluten Zeitpunkt), Bildschirm-Klasse bei `druck`.
- [x] 2.2 `components/druck/useDrucken.ts`: `drucken()` ruft `window.print()` erst, wenn die Organisationsabfrage erfolgreich war (bei Fehler Knopf gesperrt mit Wiederholen). Verifiziert durch Hook-Tests mit `vi.spyOn(window, 'print')`: kein Aufruf vor den Daten, genau ein Aufruf danach.
- [x] 2.3 Meldebild umstellen: `kraefte-nur-print`/`kraefte-druckkopf` durch den Druckkopf ersetzen (Titel „Meldebild“, Stand = ältester Listenabruf, Auswahl-Chips als Zeile), `handleDrucken` über `useDrucken` nach dem Aufklappen. Verifiziert durch die umgeschriebenen Druckkopf-Tests in `KraefteuebersichtPage.test.tsx` (vormals :816-860) und `meldebild-tabelle.spec.ts`.
- [x] 2.4 Befehl und Lagebericht umstellen: Druckkopf innerhalb der Druckwurzel (Titel „Befehl“ bzw. „Lagebericht“ mit Dokumenttitel und Status/Version als Stand), Druckknöpfe über `useDrucken`. Verifiziert durch Seitentests: Kopf vorhanden, am Bildschirm per Klasse verborgen, Knopf ruft `print` erst nach geladener Organisation; `e2e/befehl-aktionsleiste.spec.ts` bleibt grün.

## 3. Org-Branding (Backend und Verwaltung)

- [x] 3.1 PATCH `/api/organisation` mit `name` nach design.md D7. Verifiziert durch Tests in `tests/organisation_tz.rs` (Bestandsdatei der Org-Route, erweitert): Umbenennen (200, folgender GET liefert den Namen), `"   "` und 121 Zeichen → 400 und unverändert, nur `tz_organisation` wie bisher, leerer Body → 400, Nicht-Admin → 403; ein zweiter `bootstrap_admin`-Lauf lässt den gepflegten Namen stehen.
- [x] 3.2 Migration `org_logo` nach design.md D8. Vorher `git fetch`, `ls migrations` und `scripts/check-migrationen.sh`; die Nummer ist größer als die höchste auf `origin/alpha`. Verifiziert durch `cargo test --lib db` (Migrationslauf) und Repo-Tests an beiden CHECKs (Mime, Größe).
- [x] 3.3 Logo-Routen `POST|GET|DELETE /api/organisation/logo` samt `OrganisationAnzeige.logo` (`skip_serializing_if`), Routing in `app.rs` mit `DefaultBodyLimit`. Verifiziert durch `tests/organisation_logo.rs`: PNG und JPEG angenommen; SVG mit `.png`-Namen, GIF und leere Datei → 400; genau 1 MiB angenommen, 1 MiB + 1 Byte → 400; Scan-Seam gegen `127.0.0.1:1` → 503 (fail-closed), Fund-Entscheidung über `anhang::entscheide` → 422; Nicht-Admin → 403; GET mit Content-Type, ETag, `Cache-Control: private, no-cache` (nicht `immutable`), `nosniff`; `If-None-Match` → 304 ohne Body; Ersetzen ändert den ETag; DELETE → 204 und danach 404; Presence von `logo` per `contains_key` (nicht per `Value::Null`); `tests/org_scope_guard.rs` läuft mit den neuen Routen grün.
- [x] 3.4 Trennung der Organisationen: zwei Orgs mit verschiedenen Logo-Bytes, jede Person bekommt ihres; eine Org ohne Logo neben einer mit Logo → 404; ein Admin der Org A kann das Logo von B nicht ändern. Verifiziert durch Tests in `tests/organisation_logo.rs`.
- [x] 3.5 Schwärzungs-Gegenprobe: `org_logo` liegt nicht in `entdecke_einsatz_scoped`, und `schwaerze_einsatz` lässt das Logo unverändert. Verifiziert durch einen Test in `schwaerzung_registry.rs` bzw. `tests/` (Logo-Bytes vor und nach dem Schwärzen gleich).
- [x] 3.6 OpenAPI: `OrgLogoAnzeige` in `api_doc.rs`, dann `scripts/check-typ-codegen.sh`, `openapi.json` und `types.generated.ts` mitcommitten, Alias in `api/types.ts`. Verifiziert durch das grüne Codegen-Skript und `tests/enum_wire_kontrakt.rs` (falls ein Enum dazukommt).
- [x] 3.7 `api/organisation.ts`: `setzeOrgName`, `ladeOrgLogoHoch`, `entferneOrgLogo`; `setzeOrgDefault` bleibt. Verifiziert durch `api/organisation.test.ts` (Pfad, Methode, Body, Multipart-Feld `datei`).
- [x] 3.8 `stammdaten/OrganisationTab.tsx` nach design.md D9: Name mit Speichern im `<form>`, Logo mit Vorschau, Hochladen, Entfernen mit Rückfrage. Verifiziert durch `OrganisationTab.test.tsx`: Enter speichert den Namen, Fehler steht an der Seite (nicht nur im Toast), ohne Admin-Recht gesperrter Knopf mit `RechteHinweis`, Client-Vorprüfung lehnt 2 MiB ab, Entfernen fragt nach (`ant-btn-dangerous`), Abbrechen sendet nichts, jede Mutation invalidiert `globalKeys.organisation()`.
- [x] 3.9 Logo im Druckkopf verdrahten: `<img>` auf `/api/organisation/logo?v=<sha256>` nur bei gesetztem `logo`, `useDrucken` wartet zusätzlich auf `decode()`, ein Fehler druckt ohne Logo. Verifiziert durch `Druckkopf.test.tsx` (ohne `logo` kein `<img>` und kein Rahmen, mit `logo` `v`-Parameter = sha256) und Hook-Tests (`print` erst nach `decode`, bei `decode`-Fehler trotzdem genau ein `print`).

## 4. ETB-Druck (LFH-22)

- [x] 4.1 `routing/deeplinks.ts`: `etbDruckPfad(einsatzId, filter)` über `mitQuery`, Rückweg über `parseEtbFilter`. Verifiziert durch `deeplinks.test.ts`: Round-Trip durch `URLSearchParams` mit `&`, `=` und Leerzeichen im Suchbegriff; unbekannter Typ fällt ganz weg.
- [x] 4.2 `etb/druckAbruf.ts:ladeEtbVollstaendig` nach design.md D4. Verifiziert durch `druckAbruf.test.ts` mit gemocktem `listeEtb`: 1 200 Einträge in drei Aufrufen mit korrektem Cursor und `limit: 500`; genau 500 Einträge brauchen einen zweiten, leeren Aufruf; ein Fehler in Seite 3 verwirft das Ergebnis; eine Seite mit `lfd_nr` ≥ Cursor wirft; Berichtigungs-Durchgang nur bei aktivem Filter ohne `typ = berichtigung`; Fortschritt wird gemeldet; Parameter kommen nur aus `listeEtb`.
- [x] 4.3 `api/queryKeys.ts`: `einsatzKeys.etbDruck(einsatzId, filter)` mit Prefix in `EINSATZ_KEYS` und Eintrag in `NICHT_LIVE_KEYS` samt Begründung. Verifiziert durch `queryKeys.guard.test.ts`, einen Literal-Pin in `queryKeys.test.ts` und `liveEvent.contract.test.ts`.
- [x] 4.4 `etb/druckAuswahl.ts:auswahlZeilen` nach design.md D6. Verifiziert durch `druckAuswahl.test.ts`: Zeitraum beidseits beider Sommerzeitgrenzen gegen absolute Zeitpunkte (Europe/Berlin), Typwort, wörtlicher Suchbegriff, Einheit mit Namen bzw. „betrifft eine Einheit (Name nicht verfügbar)“, ohne Filter „vollständiges Tagebuch“, keine Kennung im Text.
- [x] 4.5 `pages/EtbDruckPage.tsx` und `etb/EtbDruckTabelle.tsx` nach design.md D5, Route `etb/druck` in `App.tsx`. Verifiziert durch Seitentests: Ordnung aufsteigend nach `lfd_nr`; Nachtrag an seiner Nummer mit „nachgetragen um“; „berichtigt Nr. n“, „berichtigt durch Nr. m“ auch bei Berichtigung außerhalb der Auswahl, „außerhalb dieser Auswahl“; Zeiten in der Org-Zone; „Drucken“ gesperrt während des Ladens und nach einem Fehler, Fehler mit „Erneut laden“; leere Auswahl druckbar mit Hinweis; 403 der Liste → kein Zugriff, kein Drucken; Kopf nennt Zahl, Stand (höchste Nummer) und Auswahl; genau eine Druckwurzel.
- [x] 4.6 Einstieg in `EtbPage`: sekundärer Link-Knopf „Drucken / als PDF“ im Kopf auf `etbDruckPfad` mit aktivem Filter. Verifiziert durch einen Page-Test (Ziel-URL trägt den aktiven Filter; genau eine Primäraktion in `[data-lfh="seitenkopf-aktionen"]`).
- [x] 4.7 Guards gegen die Tabellen-Ausnahme: `katalogTabelle.guard.test.ts`, `datensicht.guard.test.ts` und `statusVertrag.guard.test.ts` laufen unverändert grün; der Dateikopf von `EtbDruckTabelle.tsx` nennt die Ausnahme aus design.md D5. Verifiziert durch die volle Vitest-Suite.
- [x] 4.8 e2e `frontend/e2e/etb-druck.spec.ts` nach design.md D10: mehr als 500 Einträge gesät (parallel in Bündeln per `page.request`, mit eigenem `test.setTimeout` gegen die 2-vCPU-Shards; die Seitenlogik selbst deckt 4.2) (darunter ein Nachtrag und eine Berichtigung), Kopfzahl und letzte Nummer, aufsteigende Ordnung, gefilterter Druck mit „berichtigt durch“ und Auswahl im Kopf, unter `emulateMedia('print')` Rahmen `display: none` und Wurzel im Fluss. Verifiziert durch grünen Spec-Lauf.

## 5. Abschluss

- [ ] 5.1 Handprüfung in Firefox und Safari: Druckvorschau von Lagebericht (Lese- und Entwurfszweig, mehr als zwei Seiten), Befehl, Meldebild (mehr als eine Seite, Tabellenkopf je Seite) und ETB-Druck (mehrseitig), dazu das Logo. Chromium mit Seitenzählung als Vergleich. Screenshots oder PDF-Belege gehen in die Prüfliste.
- [x] 5.2 Prüfliste Einsatztauglichkeit `docs/superpowers/specs/2026-09-xx-lfh-22-pruefliste.md` für die ETB-Druckansicht und die umgebauten Druckansichten (Lagebericht, Befehl, Meldebild): alle 15 Kriterien mit Verdikt (erfüllt / offen → Zielticket / nicht anwendbar) und Beleg, samt Browser-Verdikt aus 5.1.
- [x] 5.3 CLAUDE.md: Absatz „Druck“ unter Frontend (Druckwurzel und gemeinsames Stylesheet, `display: none` statt `visibility`, Druckkopf und `useDrucken`, Seitenzählung nur Chromium, ETB-Druck als benannte Tabellen-Ausnahme mit Ordnung nach Nummer, Vollabruf ohne zweite Filterkopie, nicht-live Key). Die Lagebericht-Entscheidung 5 als querschnittlich gültig vermerken. Verifiziert durch Lesen des Diffs.
- [x] 5.4 Gesamtlauf `./scripts/check-all.sh` (ohne `| tail`) grün, dazu `scripts/check-migrationen.sh` gegen den frischen `origin/alpha`. Verifiziert durch die Ausgabe beider Skripte.
- [ ] 5.5 Folgetickets anlegen (Skill `clickup-task-anlegen`): Einsatzbericht als Vorlage, Druck der Modul-Listen (Personen, Tiere, Schäden) samt Frage nach Audit, Frontend-Anbindung der CSV-Exporte, e2e in Firefox/WebKit. Die IDs in proposal.md unter „Nicht-Ziele“ nachtragen. Verifiziert durch die Ticket-Links.
